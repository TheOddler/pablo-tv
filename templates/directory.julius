async function playPath(path) {
  await sendAction({ tag: "PlayPath", path });
  // Refresh the page to get the now watched info
  location.reload();
}

function playAll() {
  playPath("#{absPathJS}");
}
